run_latehook() {
  local ntpconf=/new_root/etc/ntp.conf
  if [ -f "$ntpconf" -a -n "$ntpserver" ]; then
    msg "Setting new ntpserver $ntpserver"
    grep -v "^server" "$ntpconf" > "$ntpconf"
    cat "server $ntpserver" >> "$ntpconf"
  fi
}

